import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CoachProgramsPage() {
  return (
    <PlaceholderScreen
      title="Programs"
      phase="Phase 4 · Training"
      description="Build workout templates once, reuse them across clients, and customize any client's copy without touching everyone else's program."
    />
  );
}
