import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CoachRecipesPage() {
  return (
    <PlaceholderScreen
      title="Recipes"
      phase="Phase 5 · Nutrition"
      description="Build out your recipe library here — the same recipes clients see in 'What Can I Eat?'."
    />
  );
}
