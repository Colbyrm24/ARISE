import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CoachInboxPage() {
  return (
    <PlaceholderScreen
      title="Inbox"
      phase="Phase 7 · Communication"
      description="Your unified inbox — unread, needs response, flagged, and check-in due — lands here alongside full client context."
    />
  );
}
