import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CoachClientsPage() {
  return (
    <PlaceholderScreen
      title="Clients"
      phase="Phase 2 · Client Management"
      description="Every client's profile, status, and coach notes will live here — one record per client, with everything about them in one place."
    />
  );
}
