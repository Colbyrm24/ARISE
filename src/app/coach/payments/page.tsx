import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CoachPaymentsPage() {
  return (
    <PlaceholderScreen
      title="Payments"
      phase="Phase 3 · Sales + Onboarding"
      description="Subscriptions, failed payments, and contracts pending signature will all be managed from here, across both Stripe and FanBasis."
    />
  );
}
