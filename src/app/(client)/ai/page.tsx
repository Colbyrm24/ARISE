import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function AiPage() {
  return (
    <PlaceholderScreen
      title="AI Coach"
      phase="Phase 8 · Claude"
      description="Your built-in coaching assistant — connected to your goals, macros, and program — lands here once tool calling and context retrieval are wired up."
    />
  );
}
