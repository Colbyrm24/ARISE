import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function MessagesPage() {
  return (
    <PlaceholderScreen
      title="Messages"
      phase="Phase 7 · Communication"
      description="Direct chat with your coach — text, photos, and video — lands here, built to feel like iMessage rather than customer support software."
    />
  );
}
